# Conformance — per-component table

**Generated by `node conformance/report.js`. Do not hand-edit; a hand-edited row is a bug.**

Columns: behaviour = Compose UI tests (wasmJs unless flagged `jvm-only`); a11y = missing
roles/states/attrs vs the React Aria reference from Playwright `ariaSnapshot` diffs; m3 = the
same diff for the Material3 control route (framework ceiling); CMP = the Compose Multiplatform
version the row was measured on.

| Component | Tier | Behaviour (pass/total) | A11y missing vs reference | M3 control missing | Attribution | CMP |
|---|---|---|---|---|---|---|
